language: python

env:
  global:
    - FOO=FUBU
    - FO1=BUBU
    - BOO=GUBU

  matrix:
    - M1=ONE

build:
  pre_ci:
    - echo "true4"

  pre_ci_boot:
    image_name: drydock/u14rub
    image_tag: prod
    pull: false
    options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'

  ci:
    - sleep 30
    

  post_ci:
    - echo "true"

  smoke_test:
    - sudo ls /

deploy:
  pre_build:
    - ls

