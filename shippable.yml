language: node_js

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
  pre_ci:
    - echo "true4"


  ci:
    - sleep 3

  post_ci:
    - echo "true"

  smoke_test:
    - sudo ls /

integrations:
  notifications:
    - integrationName: resource-trigger-subint
      type: webhook
      payload:
      - versionName=Shippable Build $BUILD_NUMBER
      on_success: always
      on_failure: always
      on_pull_request: always
