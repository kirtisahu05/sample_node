language: none

build:
  pre_ci:
    - echo "true4"

  ci:
    - jfrog rt u package.json test-repository-local
    - ls -al

  post_ci:
    - echo "true"

deploy:
  pre_build:
    - ls

integrations:
  hub:
    - integrationName: jfrog-integration
      type: artifactory
