# Language setting
language: node_js

env:
  global:
    - FOO=FUBU
    
build:
  pre_ci:
    - jfrog
  ci:
    - ps -eax
    - ls -al
  push:
    - jfrog

integrations:
  hub:
    - integrationName: "jfrog-subint"
      type: artifactory
