language: none

build:
  pre_ci:
    - echo "true4"
    - jfrog rt u package.json test-repository-local

  ci:
    - ls -al

  post_ci:
    - echo "true"
    

deploy:
  pre_build:
    - ls

integrations:
  hub:
    - integrationName: jfrog-integration
      type: artifactory
