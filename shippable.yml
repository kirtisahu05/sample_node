# Language setting
language: node_js

env:
  global:
    - FOO=FUBU
    
build:
  ci:
    - jfrog
    - ps -eax
    - ls -al

integrations:
  hub:
    - integrationName: "jfrog-subint"
      type: artifactory
