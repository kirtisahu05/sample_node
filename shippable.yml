language: none

build:
  pre_ci:
    - echo "true4"

  ci:
    - ls -al
    - jfrog rt u package.json test-repository-local

  post_ci:
    - echo "true"
    

deploy:
  pre_build:
    - ls

integrations:
  hub:
    - integrationName: jfrog-integration
      type: artifactory
