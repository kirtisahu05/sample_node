language: none

build:
  pre_ci:
    - echo "true4"

  ci:
    - ls -al

  post_ci:
    - echo "true"
    
  push:
    - jfrog rt u package.json test-repository-local

deploy:
  pre_build:
    - ls

integrations:
  hub:
    - integrationName: jfrog-integration
      type: artifactory
