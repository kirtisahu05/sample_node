language: node_js

env:
  global:
    - FOO=FUBU_5

  matrix:
    - M1=ONE
    - M2=TWO

build:
  pre_ci:
    - echo "true_9"

  ci:
    #- if [ "$(($BUILD_NUMBER % 2))" -eq 0 ]; then failing it; fi # fail if build number is even
    - echo "true_ci_8"
    - printenv
    - shipctl post_resource_state image versionName "build_"$BUILD_NUMBER

  post_ci:
    - echo "true"

jobs:
  - name: sample_runSh
    type: runSh
    steps:
      - TASK:
          name: print file tree
          script: sudo apt-get install tree && tree
          
resources:
  - name: test_2
    type: gitRepo
    integration: github-subint
    pointer:
      sourceName: ankul-shippable/sample_c
      branch: master


#integrations:
#  notifications:
#    - integrationName: "webhook-subint"
#      type: webhook
#      payload:
#        - state=$STATE
#        - repo_name=$REPO_FULL_NAME
#        - branch=$BRANCH
#        - build_number=$BUILD_NUMBER
#       - build_url=$BUILD_URL
##        - commit_message=$COMMIT_MESSAGE
#       - committer=$COMMITTER
#        - is_success=$is_success
#        - displayName=Shippable
#        - avatar=https://app.shippable.com/images/slack-aye-aye-yoga.png
#      on_start: always
#      on_success: always

#    - integrationName: resource-trigger-subint-2
#      type: webhook
#      payload:
#      - versionName=Shippable nginx-image-3 $BUILD_NUMBER
#      on_success: always
#      on_start: never
#      on_pull_request: always
