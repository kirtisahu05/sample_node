# Language setting
language: node_js

env:
  global:
    - FOO=FUBU
    
build:
  pre_ci_boot:
    image_name: ship-dtr.in/scriptnull/sample_node
    image_tag: beta.dtr.3
    pull: true
    
  ci:
    - jfrog
    - ps -eax
    - ls -al

integrations:
  hub:
    - integrationName: "jfrog-subint"
      type: artifactory
